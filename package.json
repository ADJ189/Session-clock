{
  "name": "session-clock",
  "version": "8.1.0",
  "description": "Beautiful animated clock — 63+ themes, Pomodoro, ambient sound, literary clock, focus log.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^6.3.5"
  }
}
